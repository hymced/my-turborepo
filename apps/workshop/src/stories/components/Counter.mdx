import { Meta, ArgTypes } from '@storybook/addon-docs'
import { Base } from './Counter.stories'

<Meta title="Components/Counter" />

# Counter

## Component properties

<ArgTypes of={Base} />

## API field

```ts
{
  variant: 'primary', // 'primary' | 'secondary'
}
```
